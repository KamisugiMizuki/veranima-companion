# veranima — 设计文档 v1

> 蓝本：`object_design/docs/newly_added/DESIGN_COMPLETE.md`（双端架构蓝图）
> 本文件是 veranima 项目的落地设计：架构 + 模块 + 技术选型 + 复用映射 + 里程碑。
> 与蓝本的区别：蓝本只定义逻辑层概念；本文落到具体技术栈与代码结构。

---

## 1. 项目定位

**桌宠 + QQ chatbot 双端**的 AI 陪伴应用。同一人格在两端的表现不同但同源：
- QQ 端：打字利落的文字朋友（IM 文字通道）
- 桌宠端：坐在旁边的语音伙伴（TTS 语音通道 + 屏幕感知）

核心目标：通过模拟真实人类的表现（认知限制 / 社交摩擦 / 时空锚定 / 表达瑕疵）增强用户的情感寄托程度。

**总设计哲学**：不追求更像 AI，追求更像「某个人」。
1. 能力有边界——「不会」和「会」同样构成真实感
2. 表达适媒介——同一意图在不同通道说出来的样子完全不同
3. 行动需翻译——Agent 执行任务的过程必须被人格化转译后才呈现给用户

---

## 2. 总体架构

**架构原则（2026-08 定案，后续所有模块实现必须遵守）**

第一层·结构约束（模块怎么切分）：
1. **分层单向依赖**：`adapters（通道/输入输出） → core（决策与协调） → memory/llm/tools（基础设施）`。禁止反向依赖，禁止跨层直调（如 adapter 直接碰 memory 的表结构）。
2. **模块边界（SRP）**：每个模块独立成篇，一个模块只有一个变化原因；只通过**明确定义的接口**（函数签名/协议/事件）交互，不共享内部状态；模块间的数据流转走「输入 → 处理 → 输出」的显式管道。例：需求翻译层只负责「模糊指令 → 结构化工单」，不直接调 API；人格化转译器只负责「状态码 → 角色口吻」，不决定何时发起对话。
3. **状态收敛**：可变状态（依恋度/耐心值/场景锁/打断计数）集中在 `core/state` 相关模块，其他模块只读或经接口更新，不各自持有副本。
4. **通道解耦（LoD）**：双端（QQ/桌宠）只依赖「语义核心层 + 通道接口」，互不知道对方存在；模块间只传递规范化数据对象（DTO），不读取对方内部状态。新增通道 = 新增一个 adapter，不触碰 core。
5. **可替换基础设施（DIP）**：LLM/embedding/记忆存储均为可替换实现（接口在前）；核心层依赖抽象接口而非具体实现。例：任务执行定义 `ITaskExecutor` 协议，桌面 Agent/本地 API/云端工具都是其插件，换实现不碰 core。

第二层·边界防御（模块间的「盔甲」）：
6. **开闭原则（OCP）**：新增主动发起机制、新增输出通道时，**不修改**核心决策引擎与表达渲染层的现有代码——通过新增配置/插件类实现。为加功能重写核心引擎的 if-else 即架构失败。
7. **接口隔离（ISP）**：不设计「万能能力接口」；能力拆为细粒度迷你接口/配置项（游戏评论/办公助理/情绪支持……），角色卡只声明自己用到的部分，防误调用、防 OOC。

第三层·演进纪律（项目的生命力）：
8. **YAGNI**：先跑通骨架再谈智能——五机制仲裁器先做优先级队列，不预建权重计算/训练管道；等真实数据积累再优化。
9. **KISS**：能用简单逻辑解决的不用复杂算法。任务/闲聊分流优先用关键词+正则规则表，规则覆盖不到的长尾才透传 LLM。
10. **组合优于继承**：角色卡 = 标准组件（性格/语气/能力/记忆偏好）组合而成，不做庞大基类+子类重写；换组件即可出新角色，不写新子类。

> 一句话：**好的架构不是为了防止出 bug，而是为了让你以后敢放心大胆地改代码。**

```
┌─────────────────────────────────────────────────────────┐
│                   输入层（多模态感知）                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  文字输入     │  │ 视觉注意力   │  │ 系统事件     │  │
│  │ （QQ/IM）    │  │ （屏幕感知） │  │ （日历/通知）│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   决策与协调层（核心）                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  角色决策引擎 │→│  需求翻译层  │→│  能力匹配层  │  │
│  │（闲聊/任务） │  │（意图补完）  │  │（权限判定）  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  记忆调度器  │  │ 社交博弈引擎 │  │ 时空沉浸引擎 │  │
│  │（分层记忆）  │  │（耐心/亲密度）│  │（共同时钟）  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   表达与输出层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 通道适配器   │→│ 表达渲染引擎 │→│ 表达瑕疵层   │  │
│  │（IM/TTS）    │  │（风格注入）  │  │（粗糙化滤镜）│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   外部协作层（暂缓）                      │
│  ┌──────────────┐                                      │
│  │ 桌面Agent    │（接收工单，执行任务，回传状态）      │
│  └──────────────┘                                      │
└─────────────────────────────────────────────────────────┘
```

**核心循环**（Filter 即仿生层）：

```
User Input → [社交博弈：消耗多少情绪资本]
           → [记忆调度：以多高确信度调取记忆]
           → [LLM 生成主干回复]
           → [时空沉浸：依场景调整节奏和篇幅]
           → [表达瑕疵：添加撤回/口癖/语法紊乱]
           → Output
```

---

## 3. 技术选型

| 层 | 选型 | 依据 |
| --- | --- | --- |
| 运行时 | Python 3.14（系统预装） | object_design 已验证 |
| LLM | OpenAI 兼容远程 API（DeepSeek/通义/硅基流动等；httpx 直调，配置 base_url/model/api_key） | 2026-08 切换：弃用本地 LM Studio |
| Embedding | `data/models/bge-m3`（sentence-transformers 本地）或 `openai:` API 模式 | object_design 已验证，1024 维 |
| QQ 接入 | aiocqhttp + NapCatQQ（OneBot v11 反向 WS，端口 8099） | object_design 已验证 |
| 桌宠前端 | **待定**（候选：Tauri v2 / Electron / WPF）。MacroPhonic 的 Tauri 桌宠端未调通（仅 TTS sidecar 可用），不能作为「已验证经验」复用，需重新评估 | 风险项，见 3.1 |
| 桌宠 TTS | Qwen3-TTS sidecar（复用 MacroPhonic 的 server.py 模式，已验证可用） | 已有实现 |
| 记忆存储 | SQLite + 向量（复用 object_design 的 store.py 模式） | 已验证 |
| 测试 | pytest | 项目惯例 |

---

## 3.1 桌宠前端选型风险（技术栈中唯一未验证项）

MacroPhonic 的 Tauri 桌宠端**未调通**（当时卡在窗口行为/GPU flags/退出码处理，最终只交付了 TTS sidecar）。因此桌宠前端不能沿用「已验证」假设，M3 前需重新选型：

| 候选 | 优势 | 风险 |
| --- | --- | --- |
| Tauri v2 | 轻量（~10MB）、Rust 后端性能好、已有部分调试经验（虽然未通） | MacroPhonic 的前车之鉴：Windows 窗口/GPU 问题调试成本高 |
| Electron | 成熟、koodo-reader 本身就是 Electron（React 栈可复用） | 体积大（~100MB+）、内存占用高；桌宠常驻场景不理想 |
| WPF/.NET | Windows 原生、常驻轻量、屏幕感知 API 直接 | 技术栈与现有 Python/TS 生态割裂 |

**决策时点**：M2（通道适配层）完成、TTS sidecar 已验证后再定，不提前绑定。核心诉求：常驻轻量 + TTS 播放 + 截屏/窗口感知 + 与 Python 后端通信（HTTP/WS 均可）。

---

## 4. 模块设计

### 4.1 角色决策引擎

**职责**：判定输入是「闲聊」还是「任务指令」，分派后续流程。

- 维护「可转交任务类型清单」（可配置）：文档处理 / 信息检索 / 系统操作 / 自动化流程
- 匹配 → 任务管道（需求翻译层）；不匹配 → 闲聊管道（记忆 + 社交 + 表达）
- **复用**：object_design 无此模块，重写（小，关键词/规则匹配）

### 4.2 需求翻译层（暂缓，随外部 Agent 一起）

**职责**：模糊指令 → 结构化任务工单 → 桌面 Agent Prompt。

- 意图补完五维：目标澄清 / 来源路径 / 用户偏好注入 / 优先级约束 / 异常预案
- 信息不足主动追问（附带猜测建议）
- 工单协议 `TASK_TRANSFER_PROTOCOL`（YAML），发送后必须给「已安排」反馈
- **里程碑**：M4，独立系统

### 4.3 能力匹配层

**职责**：判定角色「能不能做」，触发「懂但不多」模式。

- 角色卡绑定 `capabilities`：`擅长` / `略知` / `完全不懂`
- 「懂但不多」话术库：好奇型 / 共情型 / 关联型 / 走神型（每角色 ≥3 种）
- **复用**：object_design 角色卡机制（CharacterCard），扩展 capabilities 字段

### 4.4 记忆调度器

**职责**：五层记忆 + 模糊检索 + 断片机制 + 确信度驱动表达。

- 五层：短期缓存 / 情境记忆 / 长期语义 / 关系记忆 / 事件记忆
- **确信度三维度计算模型**（检索时实时折算）：
  - 时间衰减：1h 内 ×1.0；1~7 天 ×0.8；7~30 天 ×0.5；>30 天 ×0.3（除非被反复提及）
  - 情绪振幅：用户当时情绪强烈（感叹号/强烈情绪词）×1.2；平淡 ×0.7
  - 复述频率：提及 ≥3 次 ×1.3；1~2 次 ×1.0；仅 1 次 ×0.6
- **行为分级（综合确信度 → 表达范式）**：
  | 确信度 | 行为 | 表达范式（IM 示例） |
  | --- | --- | --- |
  | ≥85% | 自信调用 | 「我记得你上次说过你喜欢……」不加修饰 |
  | 60~84% | 试探性调用 | 「我好像记得你提过……是……吗？还是我记串了？」 |
  | 35~59% | 模糊关联 | 「我记得好像有这么回事……细节全糊了，你能再跟我说说吗？」（抛回给用户制造互动） |
  | <35% | 坦诚遗忘 | 「你之前说过这事吗？我完全不记得了，你再说一遍呗？」 |
- **噪声注入**：记忆细节（日期/数字/人名）表达前过模糊化滤镜——「上周三」→「就上次」、「三小时」→「半天」。完美的精确度就是非人感。**仅确信度 <84% 时生效**；高确信（≥85%）直接给精确值。用户追问细节时触发重新检索精确值（模糊化可逆，不是丢失）
- **延迟纠错**：允许后续「突然想起来」：「等一下，我刚才说错了，不是上个月，是上上个月才对」——比第一次就说对更有真实感
- 断片机制：长对话后段允许「话到嘴边想不起来」并坦然承认
- **复用**：object_design 的 memory/store.py + embedding.py（strength/confidence/decay 已存在），补三维度系数与分级表

### 4.5 社交博弈引擎

**职责**：耐心余额 + 互惠原则 + 打断决策（L0-L3 分级）。

- 情绪资本账户：深度共情消耗，低于阈值触发「节能模式」（回应变短/委婉拒绝）
- 关系亲密度：影响亲昵表达开放度（波浪号/称呼/调侃阈值）
- 互惠原则：频繁索取不分享 → 主动索取信息
- **打断决策器**（监测：话题复现计数[语义相似度归类] × 情绪深度 × 社交资本余额）：
  | 复现次数 | 资本余额 | 级别 | 行为 |
  | --- | --- | --- | --- |
  | 1~2 次 | ≥60% | L0 温和共情 | 正常安抚 |
  | 第 3 次 | 40~60% | L1 轻推式打断 | 「这事你之前说过。不过你有没有想过这样……」转向解决方案 |
  | 第 4 次 | 20~40% | L2 主动打断+主题切换 | 「我们聊点别的吧，你上次说那个游戏通关了没？」强行转移 |
  | ≥5 次或情绪无缓解 | <20% | L3 直球式回应 | 「这个问题你聊了至少五遍了。你是想听我出狠招，还是只想让我听着？后者的话我可能需要中场休息」 |
- **角色适配**：打断表达必须绑定角色卡性格（温柔学姐委婉提醒 / 毒舌游戏宅副本比喻 / 迷糊画家挠头装乱），否则 OOC
- **收尾协议**：打断不是终结对话而是转换频道——每次打断必须附带「新出口」（方案/新话题/让用户思考的问题）
- **L3 后不静默**：AI 仍响应，但转为「工作模式」（极简、就事论事、不投入情绪）——「中场休息」是态度转变不是停止回复（QQ 场景静默会被当成服务挂了）；冷却协议继续生效
- **自愈式回退**：L2/L3 打断后用户负面反馈（极短回复/终止对话）→ 冷却期 30 分钟，打断阈值提高一级
- **复用**：object_design 的 state.py（attachment 已有），补 patience 账户 + 话题复现计数器 + 打断级别；**话题/提及频率用共享 `topic_frequency` 表（语义哈希归类）——同时服务 4.4 复述频率修正与 4.5 话题复现计数，不重复建设**

### 4.6 视觉注意力调度器（桌宠端核心）

**职责**：极低成本「AI 在看着你」的感知。

- 兴趣锚点（1~3 个）：游戏血条/小地图/对话框；办公光标/地址栏/通知区
- 三态切换：稳定期（30s 低频小截图）→ 触发期（显著变化高频+扩窗）→ 游离期（2min 无变化低分辨率全屏扫描）
- 注意力惯性：3~5min 随机瞥一眼周边
- 分级管线 L0-L3：系统 API 文本状态 → 事件驱动截图 → 本地小模型筛选 → 云端/本地大模型理解
- **复用**：object_design 无此模块，重写；依赖多模态模型（已有）+ 桌面截屏能力（Tauri/Python 均可）

### 4.7 时空沉浸引擎 + 主动发起机制

**职责**：共同时钟 + 场景状态锁 + 后台心跳 + 内部动机驱动的主动发起。

- 场景状态锁：用户说「在看电影/写稿」→ 主动慢回、短回，场景结束再深聊
- 后台心跳：非对话时「离线成长」（整理聊天记录/读书），下次破冰素材；主动对话核心调度器
- **复用**：object_design 的 proactive.py（GreetingScheduler/OfflineThinkTimer）改造为场景状态驱动

**主动发起机制**（五种内部动机，替代「定时闹钟」式的生硬触发）：

| 机制 | 人性逻辑 | 触发条件 | 示例 |
| --- | --- | --- | --- |
| 联想式 | 触景生情 | 视觉锚点捕获的屏幕元素 × 事件记忆层模糊匹配（强/弱关联） | 「咦？你又开这个了？上次卡了三天的 BOSS 过了吗？」 |
| 社交疲劳 | 憋不住了 | 社交博弈引擎：倾诉量/索取量 vs 表达量比值超阈值，表达欲积压到临界 | 「等一下我得插句嘴，你说了这么多，我也有个巨离谱的遭遇憋半天了」 |
| 闲适性 | 没话找话 | 视觉稳定期 + 距上次发起超静默期 | 「好安静啊，你在干嘛呢？」（不求结果，只求在场感） |
| 时间仪式化 | 共同计时 | 时间线模块：相识满月/连续互动 N 天/第一次任务等里程碑 | 「今天是咱俩认识整整三个月的日子」 |
| 认知冲突 | 看不明白了 | 屏幕内容与用户历史行为画像冲突 / 低效重复操作 | 「嗯？你怎么突然用起这个了？上次你还骂它难用来着」 |

**发起仲裁器**：五机制并行产生请求，汇入仲裁器统一裁决：
- 打扰系数评估（场景白名单：全屏游戏战斗中/视频会议/全屏放映 → 禁止发起）
- 优先级：冲突类 > 仪式类 > 联想类 > 疲劳类 > 闲适类
- 近期发起频率限制（防刷屏）
- 连续失败抑制：连续 2 次被无视/打断 → 冷却期，时长随失败次数指数增长（模拟「热脸贴冷屁股后的识趣」）

**实现分期**：
- M0/M1：时间仪式化（里程碑扩展 OccasionChecker）+ 社交疲劳（并入 patience 账户）+ 闲适性（扩模板池）——纯 QQ 端可做
- M4：联想式 / 认知冲突 / 场景白名单——依赖视觉注意力调度器

### 4.8 表达渲染层（双端地基）

**职责**：同一意图按通道渲染。语义核心层 → 通道适配路由器 → 通道渲染器。

**核心原则：表达风格挂在输出通道上，不挂在角色卡上**——同一人格在不同媒介呈现不同质感：
QQ 里是「打字利落但有温度的朋友」，桌宠里是「坐在旁边絮絮叨叨的伙伴」。
真实感不是统一风格，而是「对话形态」与「媒介生态」的共振；在文字框里加「嗯…那个…」像演话剧，在语音里用书面语像 AI 念报告。

**IM 文字通道（QQ）**（模拟「正在打字的人」）：
- 去口语填充词（那个/就是/嗯…）；标点即情绪（句号=平静/问号=好奇/省略号=犹豫）；感叹号每段最多一次；波浪号需亲密度阈值
- 分段即呼吸：长文本拆短句+换行，模拟「打一句、想一下、再打一句」；禁止连续换行模拟「正在输入」（那是系统层的事）
- 表情符号克制植入：按性格卡设频率上限——高冷型几乎不用；温柔型每 3-5 句可带一个温和表情；活泼型可玩组合梗，但不替代文字

**TTS 语音通道（桌宠）**（模拟「正在你身边说话的人」）：
- 植入口语填充词（嗯…/那个…/怎么说呢）做呼吸感——「边想边说」的外显
- 重复与自我修正（「我昨天去了那个……哦不对，是前天」）——语音里自然，文字里累赘
- 语速标签（`<加快>`/`<低声>`/`<拖长音>`）+ `[pause=0.5s]` 停顿即留白；兴奋快+高、沮丧慢+低
- 非语义发声：允许纯语气词（嗯—/啊？/啧…）

**亲密度阈值差异化**：同一表达在不同通道的开放度不同——波浪号需文字通道关系分 >80；语音通道的语气软化可更早触发（声音比文字更能承载亲昵）。

- **复用**：object_design 的 adapters/qq.py 消息流，新增通道抽象层

### 4.9 表达瑕疵层（通道绑定 + 状态驱动）

**职责**：输出前过「粗糙化滤镜」。**瑕疵不是静态口癖，而是认知状态的外显**——高确信时流畅精准，低确信时犹豫模糊，高疲劳时草率粗糙。「这个人今天状态不错 vs 今天有点累」，就是真实感的底座。

**第一轴：确信度 → 瑕疵强度**（来自记忆调度器）：
| 确信度 | IM 文字通道 | TTS 语音通道 |
| --- | --- | --- |
| ≥85% | 语句流畅，标点精准，几乎无错字 | 语速正常，语调平稳，无填充词 |
| 60~84% | 少量语气词（嗯/其实吧），关键信息后加「对吧？」 | 语速略慢，少量填充词（那个…），不确定处轻微拖音 |
| 35~59% | 大量不确定修饰语（我好像记得/也可能是…），细节故意模糊（「大概三点左右」），允许 1~2 处错字 | 频繁填充词（嗯…/怎么说呢…），句中停顿 `[pause=0.5s]`，词句重复，音量微降 |
| <35% | 以提问主导，主动承认记忆缺失，不加掩饰 | 语速更慢，多处长停顿，「呃…」后沉默再开口 |

**第二轴：疲劳/打断 → 瑕疵强度**（来自社交博弈引擎；疲劳优先级 > 确信度——疲惫的人记清了也会懒得好好说）：
| 资本余额 | 级别 | IM 文字通道 | TTS 语音通道 |
| --- | --- | --- | --- |
| ≥60% | L0 | 正常形态 | 正常 |
| 40~59% | L1 | 回复变短，换行增多，口语化短句（行吧。/那换个话题？），语法偶尔破碎 | 语速略快，轻微叹气，语气转干脆 |
| 20~39% | L2 | 明显情绪性标点（「啧。」单独成段），句子跳脱，可插入动作描写（（敲了敲桌子）） | 短促呼吸声，语气起伏增大，咬字略重 |
| <20% | L3 | 回复极简（嗯。/知道了。），刻意错字（懒得改），不完整句子 | 语速忽快忽慢，叹气，句尾音调下降 |

**触发 L3 时所有确信度映射的修饰性瑕疵全部让位**——直接极简/粗糙输出，AI 已放弃修饰直给结论。

**通道差异化的真实感放大器**：
- IM：**错字与撤回**——低确信/疲劳时允许发出后延迟 0.5~2s 自动追加「撤回一下，我刚才打错了，应该是……」；**撤回限频 15~25%**（每次低确信都撤回比口癖更出戏），且仅当回复含具体细节时触发；标点退化（句号变空格、连续英文句号「嗯…好吧…。」）
- TTS：**非语义发声**——低确信/打断时增加「嗯-」「啧」「哎」；动态语速音量（高确信快、低确信慢+降音量、疲劳时语速不均匀）

**文本版口癖（IM 专属，替代语音填充词）**：角色卡绑定一组「句尾语气词 + 固定开场」（如小V句尾爱带「嘛」、感叹用「…啊」），每 2~3 条消息出现一次形成辨识度而非噪声；轻度错字/缩写（好哒/晓得）作为亲密期解锁项（同波浪号阈值）；口癖密度随情绪波动（兴奋变多、低落变少）。**语音填充词（嗯…那个…）禁止出现在 IM 通道**。

**动态瑕疵衰减曲线**：疲劳/低确信不永久持续——用户提供新信息、切到 AI 擅长领域、静默 30 分钟后自然恢复；瑕疵强度逐级回退（非跳变），回退过程允许「反复」（确信度回升后又短暂回落——「等等，我刚才好像又记错了……」）。

**数据流**：记忆调度器（确信度）→ 社交博弈引擎（疲劳+打断级别）→ 联动仲裁器（合并为状态标签，如「高确信+高疲劳」）→ 表达瑕疵选择器（按状态标签选瑕疵级别）→ 表达渲染层（IM 植入错字/撤回，TTS 植入填充词/停顿/语速）→ 输出

- 低配实现：随机替换 1~2% 词 / 随机插入撤回括号（按通道+状态选瑕疵池）
- **实现方式**：prompt 级引导为主（LLM 生成时直接带该通道/状态允许的瑕疵），规则后处理为辅（撤回收尾）
- **实现分期**：初期仅以状态标志形式独立运行（当前疲劳度=高/话题重复度=3 次），不实现联动权重计算；各机制跑通后再建立联动

### 4.10 人格化转译器（随需求翻译层）

**职责**：Agent 状态回传 → 角色口吻。`PROGRESS: 40%` →「那家伙手速还行，干了一半了」。

---

## 5. 与 object_design 的复用映射

| object_design 资产 | veranima 处理 |
| --- | --- |
| `memory/store.py` + `embedding.py`（五层记忆 + strength/confidence） | **复用**（拷贝 + 补断片） |
| `core/state.py`（energy/mood/attachment） | **复用**（补 patience 账户） |
| `core/character.py`（角色卡 + IDENTITY_BLOCK） | **复用**（加 capabilities/口癖字段） |
| `core/agent.py`（handle 循环 + _short_task） | **重写**（插入 Filter 仿生层） |
| `adapters/qq.py`（aiocqhttp + 防骚扰 + 表情包） | **复用**（接通道适配器） |
| `core/proactive.py`（问候/纪念日/离线思考） | **复用改造**（→ 时空沉浸引擎） |
| `llm/client.py`（httpx 直调 OpenAI 兼容 API） | **复用**（已切远程，含鉴权） |
| `core/stickers.py`（表情包库） | **复用**（QQ 端） |
| `tools/search.py`（联网搜索） | **复用** |
| `llm/prompts.py`（system prompt 组装） | **复用改造**（确信度措辞注入） |
| `core/learning.py`、`core/review.py` | **复用** |

**重写清单**：agent.py 核心循环（Filter 层）、通道适配器（新）、社交博弈 patience（新）、视觉注意力（新）、桌宠端（全新 Tauri 应用）。

---

## 6. 里程碑

| 里程碑 | 内容 | 产出 |
| --- | --- | --- |
| **M0** | 仓库初始化 + 设计文档 + 从 object_design 拷贝可复用模块 | 可跑通的单端（QQ）基础 |
| **M1** | Filter 仿生层：**实现顺序 = 记忆断片补丁（format_memory_line 已有 80%，补三维度系数）→ 打断决策（独立小模块 + topic_frequency 表）→ 表达瑕疵（prompt 引导为主，规则只做撤回收尾）** | QQ 端真实感增强 |
| **M2** | 通道适配层：语义核心 + IM/TTS 双通道渲染 | 通道抽象就绪 |
| **M3** | 桌宠端 MVP：Tauri 窗口 + TTS sidecar + 时空沉浸（场景锁/后台心跳） | 双端可用 |
| **M4** | 视觉注意力调度器（锚点/三态/L0-L3） | 桌宠屏幕感知 |
| **M5** | 需求翻译层 + 桌面 Agent（暂缓项，独立系统） | 任务管道 |

**M0 注意**：veranima 从 object_design 读取文件按需拷贝，object_design 本体不动（纯参考库）。

---

## 7. 安全边界（沿用 object_design 定案）

- **保留红线**：不参与现实社交（约定见面/寄送/代为联系）、不声称参与现实活动、不编造可证伪精确事实、不助长伤害
- **身份拟真放开**：默认自认 AI 但不主动强调；允许虚构个人经历；身份追问前两次带过第三次承认（纯 prompt）
- **表达放开**：允许问句/动作描写/轻度粗口/暧昧直白（角色卡 initial_affection 承载）
- **初始依恋度 0.5**（亲密期门槛起步）
- **项目层零内容过滤**（uncensored 本地模型，不做额外拦截）
