# veranima — 设计文档 v1

> 蓝本：`object_design/docs/newly_added/DESIGN_COMPLETE.md`（双端架构蓝图）
> 本文件是 veranima 项目的落地设计：架构 + 模块 + 技术选型 + 复用映射 + 里程碑。
> 与蓝本的区别：蓝本只定义逻辑层概念；本文落到具体技术栈与代码结构。

---

## 1. 项目定位

**桌宠 + QQ chatbot 双端**的 AI 陪伴应用。同一人格在两端的表现不同但同源：
- QQ 端：打字利落的文字朋友（IM 文字通道）
- 桌宠端：坐在旁边的语音伙伴（TTS 语音通道 + 屏幕感知）

核心目标：通过模拟真实人类的表现（认知限制 / 社交摩擦 / 时空锚定 / 表达瑕疵）增强用户的情感寄托程度。

**总设计哲学**：不追求更像 AI，追求更像「某个人」。
1. 能力有边界——「不会」和「会」同样构成真实感
2. 表达适媒介——同一意图在不同通道说出来的样子完全不同
3. 行动需翻译——Agent 执行任务的过程必须被人格化转译后才呈现给用户

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                   输入层（多模态感知）                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  文字输入     │  │ 视觉注意力   │  │ 系统事件     │  │
│  │ （QQ/IM）    │  │ （屏幕感知） │  │ （日历/通知）│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   决策与协调层（核心）                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  角色决策引擎 │→│  需求翻译层  │→│  能力匹配层  │  │
│  │（闲聊/任务） │  │（意图补完）  │  │（权限判定）  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  记忆调度器  │  │ 社交博弈引擎 │  │ 时空沉浸引擎 │  │
│  │（分层记忆）  │  │（耐心/亲密度）│  │（共同时钟）  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   表达与输出层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 通道适配器   │→│ 表达渲染引擎 │→│ 表达瑕疵层   │  │
│  │（IM/TTS）    │  │（风格注入）  │  │（粗糙化滤镜）│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   外部协作层（暂缓）                      │
│  ┌──────────────┐                                      │
│  │ 桌面Agent    │（接收工单，执行任务，回传状态）      │
│  └──────────────┘                                      │
└─────────────────────────────────────────────────────────┘
```

**核心循环**（Filter 即仿生层）：

```
User Input → [社交博弈：消耗多少情绪资本]
           → [记忆调度：以多高确信度调取记忆]
           → [LLM 生成主干回复]
           → [时空沉浸：依场景调整节奏和篇幅]
           → [表达瑕疵：添加撤回/口癖/语法紊乱]
           → Output
```

---

## 3. 技术选型

| 层 | 选型 | 依据 |
| --- | --- | --- |
| 运行时 | Python 3.14（系统预装） | object_design 已验证 |
| LLM | LM Studio 本地（`qwen3.5-9b-uncensored@q4_k_m`，多模态） | object_design 已验证，httpx 直调 OpenAI 兼容接口 |
| Embedding | `data/models/bge-m3`（sentence-transformers 本地） | object_design 已验证，1024 维 |
| QQ 接入 | aiocqhttp + NapCatQQ（OneBot v11 反向 WS，端口 8099） | object_design 已验证 |
| 桌宠前端 | **Tauri v2 + React**（桌宠窗口、TTS 播放、屏幕感知 UI） | 已有 MacroPhonic（Tauri+Python sidecar）经验可复用 |
| 桌宠 TTS | Qwen3-TTS sidecar（复用 MacroPhonic 的 server.py 模式） | 已有实现 |
| 记忆存储 | SQLite + 向量（复用 object_design 的 store.py 模式） | 已验证 |
| 测试 | pytest | 项目惯例 |

---

## 4. 模块设计

### 4.1 角色决策引擎

**职责**：判定输入是「闲聊」还是「任务指令」，分派后续流程。

- 维护「可转交任务类型清单」（可配置）：文档处理 / 信息检索 / 系统操作 / 自动化流程
- 匹配 → 任务管道（需求翻译层）；不匹配 → 闲聊管道（记忆 + 社交 + 表达）
- **复用**：object_design 无此模块，重写（小，关键词/规则匹配）

### 4.2 需求翻译层（暂缓，随外部 Agent 一起）

**职责**：模糊指令 → 结构化任务工单 → 桌面 Agent Prompt。

- 意图补完五维：目标澄清 / 来源路径 / 用户偏好注入 / 优先级约束 / 异常预案
- 信息不足主动追问（附带猜测建议）
- 工单协议 `TASK_TRANSFER_PROTOCOL`（YAML），发送后必须给「已安排」反馈
- **里程碑**：M4，独立系统

### 4.3 能力匹配层

**职责**：判定角色「能不能做」，触发「懂但不多」模式。

- 角色卡绑定 `capabilities`：`擅长` / `略知` / `完全不懂`
- 「懂但不多」话术库：好奇型 / 共情型 / 关联型 / 走神型（每角色 ≥3 种）
- **复用**：object_design 角色卡机制（CharacterCard），扩展 capabilities 字段

### 4.4 记忆调度器

**职责**：五层记忆 + 模糊检索 + 断片机制。

- 五层：短期缓存 / 情境记忆 / 长期语义 / 关系记忆 / 事件记忆
- 确信度分级：>90% 斩钉截铁；60% 附「如果我没记错」；<40% 主动反问
- 断片机制：长对话后段允许「话到嘴边想不起来」并坦然承认
- **复用**：object_design 的 memory/store.py + embedding.py（strength/confidence 已存在），补断片与确信度措辞注入

### 4.5 社交博弈引擎

**职责**：耐心余额 + 互惠原则 + 打断逻辑。

- 情绪资本账户：深度共情消耗，低于阈值触发「节能模式」（回应变短/委婉拒绝）
- 关系亲密度：影响亲昵表达开放度（波浪号/称呼/调侃阈值）
- 互惠原则：频繁索取不分享 → 主动索取信息
- 打断逻辑：同一话题重复抱怨超阈值 → 切「解决问题模式」
- **复用**：object_design 的 state.py（attachment 已有），补 patience 账户 + 打断计数器

### 4.6 视觉注意力调度器（桌宠端核心）

**职责**：极低成本「AI 在看着你」的感知。

- 兴趣锚点（1~3 个）：游戏血条/小地图/对话框；办公光标/地址栏/通知区
- 三态切换：稳定期（30s 低频小截图）→ 触发期（显著变化高频+扩窗）→ 游离期（2min 无变化低分辨率全屏扫描）
- 注意力惯性：3~5min 随机瞥一眼周边
- 分级管线 L0-L3：系统 API 文本状态 → 事件驱动截图 → 本地小模型筛选 → 云端/本地大模型理解
- **复用**：object_design 无此模块，重写；依赖多模态模型（已有）+ 桌面截屏能力（Tauri/Python 均可）

### 4.7 时空沉浸引擎

**职责**：共同时钟 + 场景状态锁 + 后台心跳。

- 场景状态锁：用户说「在看电影/写稿」→ 主动慢回、短回，场景结束再深聊
- 后台心跳：非对话时「离线成长」（整理聊天记录/读书），下次破冰素材；主动对话核心调度器
- **复用**：object_design 的 proactive.py（GreetingScheduler/OfflineThinkTimer）改造为场景状态驱动

### 4.8 表达渲染层（双端地基）

**职责**：同一意图按通道渲染。语义核心层 → 通道适配路由器 → 通道渲染器。

**IM 文字通道（QQ）**：
- 去口语填充词；标点即情绪（句号/问号/省略号）；感叹号每段一次；波浪号需亲密度阈值
- 分段即呼吸；禁止连续换行模拟「正在输入」

**TTS 语音通道（桌宠）**：
- 植入口语填充词（嗯…/那个…）做呼吸感；允许重复与自我修正
- 语速标签（`<加快>`/`<低声>`）+ `[pause=0.5s]`；允许纯语气词（啧…/啊？）

- **复用**：object_design 的 adapters/qq.py 消息流，新增通道抽象层

### 4.9 表达瑕疵层

**职责**：输出前过「粗糙化滤镜」。

- 逻辑跳跃与撤回（「不对不对，我重说」）；口癖植入（角色卡绑定）；情绪传染（负面情绪→语法紊乱）
- 低配实现：随机替换 1~2% 词 / 随机插入撤回括号
- **实现方式**：prompt 级引导为主（LLM 生成时直接带瑕疵），规则后处理为辅（撤回收尾）

### 4.10 人格化转译器（随需求翻译层）

**职责**：Agent 状态回传 → 角色口吻。`PROGRESS: 40%` →「那家伙手速还行，干了一半了」。

---

## 5. 与 object_design 的复用映射

| object_design 资产 | veranima 处理 |
| --- | --- |
| `memory/store.py` + `embedding.py`（五层记忆 + strength/confidence） | **复用**（拷贝 + 补断片） |
| `core/state.py`（energy/mood/attachment） | **复用**（补 patience 账户） |
| `core/character.py`（角色卡 + IDENTITY_BLOCK） | **复用**（加 capabilities/口癖字段） |
| `core/agent.py`（handle 循环 + _short_task） | **重写**（插入 Filter 仿生层） |
| `adapters/qq.py`（aiocqhttp + 防骚扰 + 表情包） | **复用**（接通道适配器） |
| `core/proactive.py`（问候/纪念日/离线思考） | **复用改造**（→ 时空沉浸引擎） |
| `llm/client.py`（httpx 直调 LM Studio，多模态） | **复用** |
| `core/stickers.py`（表情包库） | **复用**（QQ 端） |
| `tools/search.py`（联网搜索） | **复用** |
| `llm/prompts.py`（system prompt 组装） | **复用改造**（确信度措辞注入） |
| `core/learning.py`、`core/review.py` | **复用** |

**重写清单**：agent.py 核心循环（Filter 层）、通道适配器（新）、社交博弈 patience（新）、视觉注意力（新）、桌宠端（全新 Tauri 应用）。

---

## 6. 里程碑

| 里程碑 | 内容 | 产出 |
| --- | --- | --- |
| **M0** | 仓库初始化 + 设计文档 + 从 object_design 拷贝可复用模块 | 可跑通的单端（QQ）基础 |
| **M1** | Filter 仿生层：表达瑕疵 + 记忆断片 + 社交博弈（patience/打断） | QQ 端真实感增强 |
| **M2** | 通道适配层：语义核心 + IM/TTS 双通道渲染 | 通道抽象就绪 |
| **M3** | 桌宠端 MVP：Tauri 窗口 + TTS sidecar + 时空沉浸（场景锁/后台心跳） | 双端可用 |
| **M4** | 视觉注意力调度器（锚点/三态/L0-L3） | 桌宠屏幕感知 |
| **M5** | 需求翻译层 + 桌面 Agent（暂缓项，独立系统） | 任务管道 |

**M0 注意**：veranima 从 object_design 读取文件按需拷贝，object_design 本体不动（纯参考库）。

---

## 7. 安全边界（沿用 object_design 定案）

- **保留红线**：不参与现实社交（约定见面/寄送/代为联系）、不声称参与现实活动、不编造可证伪精确事实、不助长伤害
- **身份拟真放开**：默认自认 AI 但不主动强调；允许虚构个人经历；身份追问前两次带过第三次承认（纯 prompt）
- **表达放开**：允许问句/动作描写/轻度粗口/暧昧直白（角色卡 initial_affection 承载）
- **初始依恋度 0.5**（亲密期门槛起步）
- **项目层零内容过滤**（uncensored 本地模型，不做额外拦截）
